import { HasValuePipePipe } from './has-value-pipe.pipe';

describe('HasValuePipePipe', () => {
  it('create an instance', () => {
    const pipe = new HasValuePipePipe();
    expect(pipe).toBeTruthy();
  });
});
