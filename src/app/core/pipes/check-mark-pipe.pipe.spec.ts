import { CheckMarkPipePipe } from './check-mark-pipe.pipe';

describe('CheckMarkPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckMarkPipePipe();
    expect(pipe).toBeTruthy();
  });
});
